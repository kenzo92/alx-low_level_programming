#include "main.h"

/**
 * _isdigit - This function checks for single digits
 * @c: Parameter that copies the digit value to be checked
 *
 * Return: either 1 or 0
 */

int _isdigit(int c)
{
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
return (0);
}
