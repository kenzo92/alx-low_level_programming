#include <unistd.h>

/**
 * main - entry point
 *
 * Description: A function that writes a given
 * text input to standard error(file descriptor 2)
 * Return: Always return 1.
 */
int main(void)
{
write(2, "and that piece of art is useful\" - Dora Korpar, 2015-10-19", 59\n);
return (1);
}
