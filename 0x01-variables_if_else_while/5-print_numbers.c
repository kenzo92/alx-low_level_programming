#include <stdio.h>

/**
 * main - entry point
 *
 * Description: This Program prints all the single digit numbers
 * variable i of type int was declared.
 * Return: main returns valu of data type int
 */
int main(void)
{
int i;
for (i = 0; i <= 9; i++)
{
printf("%d", i);
}
printf("\n");
return (0);
}
